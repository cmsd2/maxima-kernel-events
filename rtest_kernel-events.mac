/* Tests for kernel-events. */
/* Real coverage lives in lisp/tests; each Lisp test runs at load */
/* time and signals on failure (see lisp/tests/runner.lisp). */

(load("kernel-events"), true);
true;

(load("lisp/tests/runner.lisp"), load("lisp/tests/all.lisp"), true);
true;
