{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/cmsd2/maxima-kernel-events/schemas/envelopes/v1/ready.json",
  "title": "ready",
  "description": "Signals the kernel is ready to accept the next evaluation. Replaces prompt-pattern detection for hosts that otherwise infer readiness from stdout.",
  "type": "object",
  "required": ["type"],
  "additionalProperties": false,
  "properties": {
    "type": { "const": "ready" }
  }
}
